import{_ as t,o,c as a,ao as s}from"./chunks/framework.Cw5ax0Rv.js";const u=JSON.parse('{"title":"How It Works","description":"","frontmatter":{},"headers":[],"relativePath":"concepts.md","filePath":"concepts.md","lastUpdated":null}'),i={name:"concepts.md"};function l(d,e,r,h,n,c){return o(),a("div",null,[...e[0]||(e[0]=[s('<h1 id="concepts" tabindex="-1">How It Works <a class="header-anchor" href="#concepts" aria-label="Permalink to &quot;How It Works {#concepts}&quot;">​</a></h1><p>This page explains the model behind the package. For step-by-step recipes, see the <a href="/PlutoShowHelpers.jl/dev/guide#guide">How-to Guide</a>.</p><h2 id="The-problem:-one-type,-three-show-methods" tabindex="-1">The problem: one type, three <code>show</code> methods <a class="header-anchor" href="#The-problem:-one-type,-three-show-methods" aria-label="Permalink to &quot;The problem: one type, three `show` methods {#The-problem:-one-type,-three-show-methods}&quot;">​</a></h2><p>Julia dispatches display through several <code>show</code> methods, and each fires in a different situation:</p><table tabindex="0"><thead><tr><th style="text-align:right;">Method</th><th style="text-align:right;">When it fires</th></tr></thead><tbody><tr><td style="text-align:right;"><code>show(io, x)</code></td><td style="text-align:right;">2-arg: string interpolation, <code>repr(x)</code>, compact contexts</td></tr><tr><td style="text-align:right;"><code>show(io, MIME&quot;text/plain&quot;(), x)</code></td><td style="text-align:right;">3-arg: REPL display, multi-line output</td></tr><tr><td style="text-align:right;"><code>show(io, MIME&quot;text/html&quot;(), x)</code></td><td style="text-align:right;">HTML rendering (Pluto, Jupyter, docs)</td></tr></tbody></table><p>Writing all three by hand means three places to keep in sync, and the third one has to branch further: an interactive Pluto tree is a very different artefact from the static HTML that Jupyter or a documentation build wants.</p><p>PlutoShowHelpers collapses this into a single customization point. You describe <em>what</em> to show as a <code>NamedTuple</code>, and the package decides <em>how</em> to render it in each context.</p><h2 id="The-two-context-split" tabindex="-1">The two-context split <a class="header-anchor" href="#The-two-context-split" aria-label="Permalink to &quot;The two-context split {#The-two-context-split}&quot;">​</a></h2><p>Everything HTML-related keys off one question: is this <code>IO</code> inside a Pluto notebook? <code>is_inside_pluto(io)</code> answers it, and the two singleton types <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.InsidePluto"><code>InsidePluto</code></a> and <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.OutsidePluto"><code>OutsidePluto</code></a> let you dispatch on the answer.</p><p>Inside Pluto, output becomes Pluto&#39;s native collapsible tree widget, where a field can be visible when collapsed, visible only when expanded, or never visible. Outside Pluto, there is no tree — output falls back to the compact 2-arg form, which is why the compact and &quot;collapsed&quot; columns of the visibility table behave alike.</p><h2 id="The-routing" tabindex="-1">The routing <a class="header-anchor" href="#The-routing" aria-label="Permalink to &quot;The routing {#The-routing}&quot;">​</a></h2><p><a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.CustomShowable"><code>CustomShowable</code></a> is the abstract type that carries the routing. Its <code>text/html</code> method inspects the <code>IO</code> and forwards to either <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.show_inside_pluto"><code>show_inside_pluto</code></a> or <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.show_outside_pluto"><code>show_outside_pluto</code></a>. Two concrete subtypes ship with the package:</p><ul><li><p><a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.DefaultShowOverload"><code>DefaultShowOverload</code></a> — the full pipeline. It reads <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.show_namedtuple"><code>show_namedtuple</code></a> and renders it appropriately for all three <code>show</code> methods. <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.@default_show_overload"><code>@default_show_overload</code></a> simply forwards your type&#39;s <code>show</code> methods to it.</p></li><li><p><a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.AsPlutoTree"><code>AsPlutoTree</code></a> — the tree renderer alone, for when you want to hand-write the non-Pluto HTML but keep the tree inside Pluto.</p></li></ul><p>Subtyping <code>CustomShowable</code> directly gets you the same routing without going through <code>DefaultShowOverload</code>, which is the escape hatch when the <code>show_namedtuple</code> model does not fit your type.</p><h2 id="Why-a-NamedTuple" tabindex="-1">Why a <code>NamedTuple</code> <a class="header-anchor" href="#Why-a-NamedTuple" aria-label="Permalink to &quot;Why a `NamedTuple` {#Why-a-NamedTuple}&quot;">​</a></h2><p><a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.show_namedtuple"><code>show_namedtuple</code></a> returns data, not formatted text. That indirection is what lets one definition serve three contexts: the same tuple becomes <code>Sat(&quot;ISS&quot;, 408.0)</code> in compact form, a labelled block in the REPL, and a tree in Pluto.</p><p>It also composes. Values in the tuple need not be the original field values — they can be computed summaries, wrapped in visibility markers like <a href="/PlutoShowHelpers.jl/dev/api#PlutoShowHelpers.HideWhenCompact"><code>HideWhenCompact</code></a>, or wrapped in <code>DefaultShowOverload</code> to recurse into a nested custom type without committing type piracy on someone else&#39;s struct.</p><p>Field <em>labels</em> come from the tuple&#39;s keys, which is why suppressing a label uses Julia&#39;s gensym key syntax (<code>var&quot;#x&quot;</code>) rather than a separate mechanism — an invisible label is still a distinct key.</p><h2 id="CONTEXT" tabindex="-1">CONTEXT <a class="header-anchor" href="#CONTEXT" aria-label="Permalink to &quot;CONTEXT {#CONTEXT}&quot;">​</a></h2><div class="warning custom-block"><p class="custom-block-title">Experimental API</p><p>The <code>CONTEXT</code> mechanism is experimental and may change in future versions.</p></div><p><code>CONTEXT</code> is a package-scoped <code>ScopedValue</code> holding a <code>Dict{Any,Any}</code>, set for the duration of a show call.</p><p>It exists because <code>show_namedtuple</code> returns only field data, yet occasionally a type needs to influence rendering decisions that sit outside that data — the current supported case being which fields keep their labels in the compact 2-arg form. Rather than widening the <code>show_namedtuple</code> return type for every such knob, the scoped dictionary acts as a side channel between <code>show_namedtuple</code> and the renderer downstream of it.</p><p>The trade-off is deliberate and not free: it is implicit state, invisible in the function signature, and the reason the API is marked experimental. Prefer expressing intent through the returned <code>NamedTuple</code> where you can. See <a href="/PlutoShowHelpers.jl/dev/guide#howto-context">Print field labels in compact show</a> for the concrete usage.</p>',23)])])}const m=t(i,[["render",l]]);export{u as __pageData,m as default};
